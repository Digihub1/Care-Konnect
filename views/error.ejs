<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <%- include('partials/header') %>

    <div style="padding: 4rem 2rem; text-align: center; min-height: 60vh; display: flex; align-items: center; justify-content: center;">
        <div class="container">
            <i class="fas fa-exclamation-triangle" style="font-size: 4rem; color: var(--kenya-red); margin-bottom: 2rem;"></i>
            <h1 style="color: var(--kenya-red); margin-bottom: 1rem;">Oops! Something Went Wrong</h1>
            <p style="font-size: 1.2rem; color: var(--gray-medium); margin-bottom: 2rem;">
                <%= message || 'An unexpected error occurred. Please try again later.' %>
            </p>
            
            <% if (process.env.NODE_ENV === 'development' && error) { %>
                <div style="background: #f5f5f5; padding: 2rem; border-radius: 10px; margin: 2rem 0; text-align: left; overflow-x: auto;">
                    <h3 style="color: var(--kenya-red); margin-bottom: 1rem;">Error Details (Development Only)</h3>
                    <pre style="background: white; padding: 1rem; border-radius: 5px; overflow-x: auto;"><%= error.stack %></pre>
                </div>
            <% } %>

            <div style="display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap;">
                <a href="/" class="btn" style="background: var(--kenya-green); color: white; text-decoration: none;">
                    <i class="fas fa-home"></i> Go Home
                </a>
                <button onclick="window.history.back();" class="btn" style="background: var(--kenya-red); color: white; border: none; cursor: pointer;">
                    <i class="fas fa-arrow-left"></i> Go Back
                </button>
            </div>
        </div>
    </div>

    <%- include('partials/footer') %>
    <script src="/js/script.js"></script>
</body>
</html>
